version: '3.9'
services:
  library-frontend:
    build: .
    image: library-image:v1
    container_name: library-webapp
    environment:
      - MONGODB_URI=mongodb://library-db:27017/libraryDB
      - DB_NAME=libraryDB
    ports:
      - "3005:3000"
    depends_on:
      - library-backend

  library-backend:
    image: mongo:latest
    container_name: library-db
    ports:
      - "27018:27017"
    volumes:
      - ./data:/data/db


  

    